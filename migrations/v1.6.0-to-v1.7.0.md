# v1.6.0 to v1.7.0

Removes the spike term, binds the exact `fork-operations` skill name, defines
Oracle completeness strictly within bounded Promise Scope, prefers one completed
file per commit except protocol-required atomic transitions, and requires
contiguous Git-parseable checkpoint trailers.
